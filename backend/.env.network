# Network Configuration for Local Network Access
PORT=5000
NODE_ENV=production

# Database Configuration
DB_TYPE=sqlite
DB_PATH=./database.sqlite

# Security - CHANGE THIS!
JWT_SECRET=cba-portal-secret-key-change-this-in-production

# Network Access Settings
HOST=0.0.0.0
CORS_ORIGIN=*

# Allow connections from any IP on local network
BIND_ADDRESS=0.0.0.0