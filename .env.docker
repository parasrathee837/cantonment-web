# Docker Environment Configuration for CBA Portal
# This file contains environment variables for Docker containers

# PostgreSQL Database Configuration
POSTGRES_DB=cba_portal
POSTGRES_USER=cba_admin
POSTGRES_PASSWORD=CBA@2025Portal
POSTGRES_INITDB_ARGS=--encoding=UTF8 --locale=C

# Backend Application Configuration
NODE_ENV=production
PORT=5000
DB_TYPE=postgresql
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cba_portal
DB_USER=cba_admin
DB_PASSWORD=CBA@2025Portal

# Security Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_here_change_this_in_production
CORS_ORIGIN=http://localhost:3000,http://localhost,http://127.0.0.1:3000

# Redis Configuration (if using)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CBA@2025Redis

# PgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@cba.local
PGADMIN_DEFAULT_PASSWORD=CBA@2025Admin
PGADMIN_CONFIG_SERVER_MODE=False

# Backup Configuration
PGPASSWORD=CBA@2025Portal

# Docker Network Configuration
COMPOSE_PROJECT_NAME=cba-portal
DOCKER_BUILDKIT=1