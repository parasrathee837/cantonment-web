# CBA Portal - Windows Environment Configuration
# Copy this file to .env before running docker-compose

# Application Configuration
NODE_ENV=production
PORT=5000
TZ=UTC

# Database Configuration
DATABASE_PATH=/app/backend/database.sqlite
UPLOAD_PATH=/app/uploads

# Windows-specific paths (adjust according to your Windows setup)
# Use forward slashes (/) even on Windows for Docker compatibility
CBA_UPLOADS_PATH=./data/uploads
CBA_LOGS_PATH=./data/logs
CBA_DATABASE_PATH=./data/database
CBA_BACKUPS_PATH=./data/backups

# Optional: SSL Configuration (uncomment if using HTTPS)
# SSL_CERT_PATH=./certs/cert.pem
# SSL_KEY_PATH=./certs/key.pem

# Optional: External database (if not using SQLite)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=cba_portal
# DB_USER=cba_user
# DB_PASSWORD=your_secure_password

# Security Configuration
JWT_SECRET=your_jwt_secret_key_here_change_in_production
ADMIN_EMAIL=admin@cba.local
ADMIN_PASSWORD=admin123_change_this

# Backup Configuration
BACKUP_RETENTION_DAYS=7
BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM

# Windows Docker Desktop Settings
COMPOSE_CONVERT_WINDOWS_PATHS=1